<h1>Sitemap</h1>

<ul class="govuk-list">
  <% @pages.each do |slug, page| %>
    <li><%= govuk_link_to page.dig(:front_matter, :title), slug, no_visited_state: true %> - Last updated: <%= page.dig(:last_updated_at) %></li>
  <% end %>
</ul>
