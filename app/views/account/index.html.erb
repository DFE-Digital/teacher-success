<% content_for :page_title, "Account #{current_user.first_name} #{current_user.last_name}" %>

<div class="govuk-width-container">
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l"><%= "#{current_user.first_name} #{current_user.last_name}" %></h1>
    </div>
  </div>
</div>