apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teach-temp.test.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
    - host: teach-temp.pr-206.test.teacherservices.cloud
      http:
        paths:
          - pathType: ImplementationSpecific
            backend:
              service:
                name: teach-pr-206
                port:
                  number: 80
